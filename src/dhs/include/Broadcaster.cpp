#include "Broadcaster.h"

/*Broadcaster::Broadcaster(const std::string& topic_name) {
	publisher_ = publisher_node_.advertise<cv_bridge::CvImagePtr>(topic_name,3);
}

void Broadcaster::Broadcast(const cv::Mat& frame) {
	cv_bridge::CvImagePtr msg;
	msg->image = frame;
	publisher_.publish(msg);
}
*/
